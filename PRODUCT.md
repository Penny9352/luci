# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Vite + React + TypeScript，纯 CSS（CSS 自定义属性），无 UI 框架。用户选定。理由：与产品端两端一致（`lucimix.com` 与 `biz.lucimix.com` 均为 Vite + React），团队维护无切换成本，产品 design token 可直接继承；后续 P1 的应用点落地页需由 30 条素材数据驱动生成，React 更合适。

## Users

主体是**个人用户**，光谱很宽，产品原则是「入口很宽，天花板不低」：

| 类型 | 情境与要做的事 |
|---|---|
| 纯小白 | 从不关心模型，打开就问。写文案、查资料、问问题 |
| 日常效率型 | 有重复性 AI 需求（日报、信息汇总、文件处理），要它定时发生 |
| 深度用户 | 会挑模型、比较结果、在意产出管理 |
| 技术 / 开发者 | 有自己的工具链，需要 API 接入自有工具 |
| 外部应用商家 | 有 AI 应用产品，需要分发与变现渠道 |

**工作与生活并重**，不是纯职场效率工具。产品首页的快捷提问同时覆盖商务邮件、育儿英语启蒙、家常菜、区块链科普、家电选购、初创公司 PMF。

## Product Purpose

Lucimix 是面向个人用户的 **AI 工作台**：聊天是起点，但产出能沉淀、任务能自动化、常用聊天 App 里也能用。同时作为**平台**承载开发者上架的外部应用，提供统一的用户体系与计费结算。

成功 = 访客理解「凭什么是你」并完成注册。所处市场里豆包、Kimi、元宝、纳米 AI 全部免费，「免费开始使用」本身没有说服力——用户不缺一个免费 AI 对话框，他已经有三个了。

## Positioning

三条邻近产品无法照抄的机制：

1. **交叉验证** —— 多个模型背靠背独立作答，系统自动合并去重，一致的沉淀为共识，不一致的标记为冲突。豆包 / Kimi / 元宝**结构上做不到**：单个模型在编造时不会告诉你它在编，这是模型自身的盲区，不是能力差距。
2. **云端定时任务 + IM 推送** —— 任务在云端按时执行，结果直推微信 / 飞书 / 企业微信，手机锁屏也照样到。豆包 / Kimi / 元宝**完全没有推送能力**——你不去问，它就不存在；WorkBuddy 的自动化要装客户端、电脑常开。
3. **一个账号、一份光点通用** —— 平台上的应用不用各自注册、各自充值。与「下载一堆 AI 小工具、每个都要单独付费」的直观区别。

## Operating Context

- **多端**：网页端 + 微信 / 飞书 / 企业微信扫码绑定，IM 中直接对话，文件自动同步回网页端
- **计费**：「光点」积分制，微信 / 支付宝扫码充值；侧边栏常驻余额与快捷充值；邀请好友双方各得 1,000 光点
- **产品导航**：个人助手 / 文件库 / 定时任务
- **注册**：手机号 + 短信验证码，未注册自动创建账号（`lucimix.com/login`）
- **企业版**独立存在于 `biz.lucimix.com`，是**另一个产品与另一个站**，本项目不涉及

## Capabilities and Constraints

### 四层能力

1. **模型调度** —— 智能模式（自动选单模型）/ 交叉验证（多模型聚合）/ 自定义模型。接入 DeepSeek、Kimi、GLM、Qwen 等国产模型，以及 Claude、GPT 等世界前沿模型
2. **产出可管理** —— 文件库（全部 / 我的云盘 / 对话内文件 / 图片生成 四 tab）、工作区侧栏、从资料库引用到对话、图片生成、对话历史管理
3. **融入日常** —— IM 绑定、定时任务（标题 ≤25 字、描述 ≤500 字、频率 + 时刻、推送渠道、每个任务单独指定模型）、API Key（OpenAI 兼容）、光点充值、邀请奖励、用量统计
4. **平台生态** —— 外部应用上架、统一用户体系、集中计费结算

### 严禁写入任何对外物料的场景

超出产品当前能力，写了会造成预期落差或合规问题：

Excel 多表合并 / 数据清洗 / 透视表；**PPT 自动生成并保存到本地**（国内 AI 工具最大的单一心智，竞品该场景内容普遍 6000+ 赞，但我们确实做不了）；PDF / 扫描件 / 合同批量处理与格式转换；会议录音转写与说话人区分；本地文件批量重命名 / 整理 / 远程控制客户端；建站 / 部署；Skill 市场 / 插件生态；漫剧 / 绘本 / 多角色连续分镜；降 AIGC 检测率 / 降重 / 代写论文（能力无效 + 合规违规）；「搭建工作台」（无配置保存与一键复用机制）。

### 未决产品事实（不得当作已有能力书写）

| 事项 | 卡住什么 |
|---|---|
| 定时任务执行时**能否联网检索** | 「每天早上自动检索行业动态」类表述。产品自身的任务描述占位符写着「关注当天 AI 领域重要动态，筛选 3-5 条并说明理由」，是有力旁证，但未经研发确认 |
| 定时任务**能否引用文件库文件** | 「根据我本周记录的工作内容」类表述 |

### 命名

产品内部有两处不一致，对外统一取前者：**定时任务**（侧边栏）而非「自动任务」（页面标题）；**交叉验证**（对外名、进度条已用）而非「暴力模式」（输入框 chip、内部名）。

## Brand Commitments

- **主体**：北京光忽智能科技有限公司
- **名称来处**：《道德经》「道之为物，惟恍惟惚」。品牌页动画把「恍惚」的竖心旁抹掉得到「光忽」，落到「光忽智能」。这是真实的品牌资产，不是装饰
- **C 端 logo**：`assets/brand/C端LOGO.svg`，橙色渐变 `#FF8559 → #EF682D`，无底板。形态是两片向中心收束的翼 + 中间一个方核——多路汇聚成一个核心，与「聚合」同构。`assets/brand/lucimix-logo.svg`（蓝 `#0052ff` + 圆角方底）是 B 端 / 通用标记，**C 端官网不使用**
- **字标**：小写 `lucimix`，字体待确认
- **已上线且沿用的文案**：主标题「你的专属 · 全能 AI 工作台」（登录页）
- **用户指定的绑定视觉约束**：暖暗场基调 + 品牌橙 `#ff6b00` + 金 `#d4b87a` 点缀；参考 Claude 首页的橙 / 黑 / 点缀方案；结构参考 workbuddy.ai（首屏上半讲定位、下半直接放产品）；不做吉祥物

### 话术纪律

| ✅ 用 | ❌ 不用 | 原因 |
|---|---|---|
| 交叉验证、分歧标红、多模型背靠背 | **多模型对比** | 该词在小红书已被天梯图 / 评测内容占据 |
| 智能模式 / 自定义模型 | **暴力模式** | 内部叫法，不对外 |
| 具体动作与产出 | 更强 / 最优 | 空洞 |
| **「出错的地方会被标出来」** | 「保证正确」 | 不可承诺 |
| 「不用露脸、不用出镜」 | 「看不出是 AI」 | 前者是事实，后者是用户自己判断的事 |

**必须出现的免责**：股票复盘类内容为信息整理工具，**不提供任何投资建议**；AI 形象生成只能用本人照片或有授权的形象（肖像权风险落在平台）。

## Evidence on Hand

**真实产品截图**（`assets/brand/`）：

| 文件 | 内容 | 状态 |
|---|---|---|
| `lucimix-c-交叉验证-暗色.png` | 交叉验证全流程，暗色主题。阶段一三个模型并行作答（GLM 5.1 / Sonnet 4.6 / DeepSeek V4 Pro，带字数），阶段二交叉验证推理日志 | **最强素材**。日志里「冲突检测：成本结论存在分歧（GLM 认为更省，Sonnet 认为更贵）」「剔除 2 处无法被交叉印证的具体数字」是证据而非宣传语 |
| `暴力模式交叉比对.png` | 同上，浅色主题 | 临时。输入框 chip 含禁用词「暴力模式」 |
| `lucimix_c_首页.png` | 个人助手首页，浅色 | 临时 |
| `lucimix-C-文件库.png` | 文件库，浅色 | 临时。库内仅 1 个文件且是竞品文档，需替换为填充版 |
| `lucimix-c-创建定时任务.png` | 创建任务弹窗，浅色 | 临时。空表单（0/25、0/500、三渠道全未绑定），需替换为配置好的版本 |

用户已授权：含禁用词与空状态的图先用，后续替换。

**尚不存在、不得伪造**：微信收到定时任务推送的手机截图（先做成明确属于本站设计的示意图，不伪造成真实微信截图）；横版组合标（logo + wordmark）；ICP 备案号（品牌页上的 `京ICP备2025000000号` 是占位）。

**文字素材**：`Lucimix产品运营/应用点素材库.md`（30 条应用点，每条含场景 + 提示词 / 产品能力 / 人群标签 / 预期产出）；`Lucimix产品介绍/product-brief-c.md`；竞品全景清单 26 家；企业销售 PDF 12 页（B 端，本项目不用）。

**没有的东西**：真实用户证言、客户 logo、用户量 / 营收数字、第三方评测、案例研究。全部不得编造。

## Product Principles

1. **先回答「凭什么是你」，再谈注册。** 竞品全部免费，差异不讲清楚，CTA 就是白放的。
2. **入口很宽，天花板不低。** 小白开箱即用无需配置；深度用户有模型选择、文件库、定时任务、API 等进阶能力。两端都不能牺牲。
3. **端到端闭合。** 对话 → 产出 → 管理 → 复用，不依赖外部工具。
4. **只承诺做得到的。** 宁可少讲一个场景，不可让用户带着错误预期进来。禁区清单是硬约束，不是建议。
5. **用证据代替形容词。** 交叉验证的推理日志比任何一句「更准确」都有说服力。

## Accessibility & Inclusion

- 中文为主要语言；国际化（多语言、Stripe/PayPal、GDPR、IM 渠道换成 WhatsApp/Slack/Teams）是未来事项，当前不做
- 正文对比度 ≥ 4.5:1；非文本 UI 元素 ≥ 3:1
- 全部动效必须响应 `prefers-reduced-motion: reduce`，且关闭动效后内容完整可读
- 键盘完全可达；产品截图配中文 `alt`
