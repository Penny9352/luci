# Lucimix C 端官网 · 首页设计规格

> 2026-08-19
> 前置文档：[../../01-现状分析-视觉与内容.md](../../01-现状分析-视觉与内容.md)
> 范围：C 端官网**首页一整页**。不含应用点落地页、定价页、开发者页。

---

## 1. 目标与范围

### 1.1 这一页要做成的事

按优先级：

1. **说清「凭什么是你」** —— 访客多半已经在用豆包/Kimi/元宝，它们全部免费。「免费开始使用」在这个市场没有说服力，页面必须先回答差异，再谈注册。
2. **拉注册** —— 主 CTA 全站统一，贯穿七个区块。
3. **展示产品全貌** —— 让人看到界面，降低"这到底是什么"的不确定感。

### 1.2 明确不在本次范围内

- 应用点落地页模板（P1，承接小红书/抖音投放，另起一轮）
- 定价页、开发者页、生态应用详情页（P2，本次仅导航留位或锚点）
- B 端企业站（第二阶段，独立项目）
- 英文/海外版

### 1.3 成功标准

- 首屏在 1440×900 视口内，定位、副标题、双 CTA、模拟对话窗口顶部全部可见，无需滚动
- 页面所有对外表述均可回溯到 `01-现状分析` §2 的产品事实，且不触碰 §5.1 禁区
- 移动端（375px）七个区块全部可用，模拟对话与粒子动画有降级方案
- 首屏 LCP < 2.5s（4G），`prefers-reduced-motion` 下所有动画停止且内容完整

---

## 2. 已定决策

| # | 决策 | 出处 |
|---|---|---|
| 1 | B 端与 C 端是两个完全独立的站 | 用户 |
| 2 | 先做 C 端 | 用户 |
| 3 | 首屏走「一站式 AI 工作台」完整定位，不用单一卖点扒首屏 | 用户 |
| 4 | 视觉基调黑金（`#000` + `#d4b87a`），电蓝 `#0052ff` 收窄到 CTA 与交互态 | 用户 |
| 5 | **结构**参考 WorkBuddy：首屏上半讲定位，下半直接把产品放出来 | 用户 |
| 6 | 首屏下半的模拟对话演**一轮普通对话**（不是交叉验证） | 用户 |
| 7 | 首页含**生态应用**区块 | 用户 |
| 8 | 第一版一次做完整页首页 | 用户 |
| 9 | 首屏产品展示用重建的黑金版模拟对话；首屏以下功能区用真实截图 | 见 §5.1 |
| 10 | 不做吉祥物 | 见 §3.5 |

---

## 3. 视觉系统

### 3.1 色板

黑金承担基调，电蓝承担动作。金色在本站有**语义职责**，不是装饰色：金 = 需要你注意的地方（分歧、强调、数据）；白 = 已经确定的地方（共识、正文）。

```css
:root {
  /* 底 */
  --bg:            #000000;   /* 页面底 */
  --bg-elev:       #0a0a0c;   /* 区块交替底 */
  --surface:       #141417;   /* 卡片、窗口 */
  --surface-2:     #1c1c20;   /* 卡片内嵌层 */

  /* 描边 */
  --border:        #ffffff14;
  --border-strong: #ffffff29;
  --border-gold:   #d4b87a3d;

  /* 文字 */
  --text:          #ffffff;   /* 21:1  */
  --text-2:        #a3a3a8;   /* 8.4:1 */
  --text-3:        #83838a;   /* 5.6:1 */

  /* 金 —— 强调与"需注意" */
  --gold:          #d4b87a;
  --gold-bright:   #e8d4a2;
  --gold-dim:      #d4b87a80;
  --gold-glow:     #d4b87a1f;

  /* 蓝 —— 仅动作 */
  --blue:          #0052ff;
  --blue-hover:    #1a66ff;
  --blue-glow:     #0052ff33;
}
```

**用色纪律**

- `--blue` 只出现在：主 CTA 按钮、logo 标记、链接 hover、输入焦点环。不用于标题、图标、装饰。
- `--gold` 用于：标题中的重点词、分歧/冲突标记、数字与数据、区块序号、分隔线。
- 大面积金底禁止 —— 金只作前景与细线。
- 产品截图放进 `--surface` 内嵌框，加 `--border-gold` 细描边，与页面隔离，不直接平铺在黑底上。

### 3.2 字体

| 用途 | 字体 | 备注 |
|---|---|---|
| 中文标题（衬线） | **思源宋体 / Noto Serif SC** | 免费可商用。`QingNiao` 是子集字体，正文不可用（见 01 文档 §1.3） |
| 中文正文 | Noto Sans SC → `PingFang SC, Hiragino Sans GB, Microsoft YaHei` | 继承产品字体栈 |
| 拉丁 | Inter | 产品在用 |
| 数字 | JetBrains Mono，500 | 继承产品 `--ff-number`，用于模型数量、光点、用量 |

**中文衬线必须做子集化**：全字符集思源宋体 5MB+，直接引入会毁掉 LCP。构建期扫描所有标题文案，用 `fonttools` 子集化，目标 < 60KB woff2。这是实现阶段的必办项，不是优化项。

### 3.3 排版阶梯

继承产品 token 并向上延伸（官网需要比产品更大的展示级字号）：

| 名 | 尺寸 | 用途 |
|---|---|---|
| `--fs-display` | `clamp(40px, 6vw, 76px)` | 首屏主标题 |
| `--fs-h1` | `clamp(30px, 4vw, 46px)` | 区块标题 |
| `--fs-h2` | `clamp(22px, 2.4vw, 30px)` | 子标题 |
| `--fs-lead` | `clamp(16px, 1.4vw, 20px)` | 首屏副标题、区块导语 |
| `--fs-body` | `16px` | 正文（= 产品 `--fs-body-md`） |
| `--fs-sm` | `14px` | 辅助 |
| `--fs-cap` | `13px` | 标注（= 产品 `--fs-caption`） |

圆角、阴影直接沿用产品阶梯：`4 / 8 / 12 / 16 / 20 / 24`。

### 3.4 背景与动效

- **粒子层**：复用品牌开场页的 canvas 方案（源码已存 `assets/brand/brand-splash-lucimix.com.html`）。参数沿用：20–90 个星点 + 2–3 个巨型极淡光斑，景深视差（大光斑速度 ×0.6），约 16% 金色粒子，约 45% 小点为实心核。**仅在首屏渲染**，离开视口即 `cancelAnimationFrame`。
- **滚动进场**：区块以 `IntersectionObserver` 触发，`opacity + translateY(16px)`，280ms `ease-out`，一次性。不做视差滚动。
- **`prefers-reduced-motion: reduce`**：粒子不启动，模拟对话直接显示终态全文，进场动画取消。所有内容仍完整可读。

### 3.5 不做的事

- **吉祥物** —— WorkBuddy 有机器猫，我们不做。黑金 + 道德经的调性与吉祥物互斥。
- 视差滚动、鼠标跟随光效、滚动劫持。
- 大面积渐变与玻璃拟态。金色细节 + 黑底留白已经足够，再加会脏。

---

## 4. 页面结构

七个区块，顺序即说服顺序：定位 → 凭什么 → 怎么融入生活 → 产出去哪了 → 用的是谁的模型 → 平台是什么 → 开始用。

### 导航

固定顶栏，滚动 8px 后加 `backdrop-filter: blur(12px)` 与底部细线。

```
▮ lucimix    交叉验证  定时任务  生态应用  定价        登录  [ 免费使用 ]
```

- 前三项为页内锚点。「定价」在 v1 也做成锚点（指向 §4.7 尾部 CTA 区内的光点说明），**不留死链**。
- 「免费使用」= 主 CTA，`--blue` 实心；「登录」为文字链。
- 两者均指向 `https://lucimix.com/login`（待确认，见 §8）。
- 移动端折叠为汉堡菜单，主 CTA 常驻不折叠。

---

### 4.1 首屏 · 定位 + 模拟对话

**结构**（参考 WorkBuddy 的上下分工）

```
┌─────────────────────────────────────────────────┐
│  [导航]                                          │
│                                                 │
│           你的专属 · 全能 AI 工作台                 │  ← 中文衬线，--fs-display
│                                                 │     「全能」二字用 --gold
│      副标题（见下），--fs-lead，--text-2           │
│                                                 │
│     [ 免费开始使用 ]    [ 看它怎么用 ↓ ]            │
│                                                 │
│   ╭───────────────────────────────────────╮     │  ← 从底部升起，露出上 2/3
│   │ ● ● ●                        模拟对话  │     │     被视口底裁切
│   │                                       │     │
│   ╰───────────────────────────────────────╯     │
└─────────────────────────────────────────────────┘
```

**主标题**：`你的专属 · 全能 AI 工作台`

沿用登录页已上线文案（见 01 文档 §2.1），不另起。「全能」用金色。

**副标题**（实现按此，不再取舍）：

> 国产与世界前沿模型全部接入。产出自动沉淀，任务定时执行，结果直接送到微信。

三句都是可验证的事实，且三句竞品都做不到（单一厂商无法"全部接入"；豆包/Kimi/元宝无产出库、无推送能力）。

已考虑并否决的备选，记录在此以免重开：

> ~~别的 AI 你不问，它就不存在。Lucimix 会自己找你。~~
> 更锋利，但把「推送」提到首位，与本区块演一轮普通对话（决策 6）的安排错位。该句改用在 §4.3 的导语里。

**CTA**

- 主：`免费开始使用` → 登录/注册页，`--blue` 实心，圆角 12px
- 次：`看它怎么用 ↓` → 锚点滚动到 §4.2，描边按钮，`--border-strong`

**模拟对话窗口**

- 深色窗口（`--surface`）+ `--border-gold` 细描边 + 顶部三个圆点，宽 `min(1040px, 92vw)`，从视口底部升起并被裁切，暗示"下面还有"。
- 自动播放**一轮普通对话**（决策 6），内容取自素材库 `B1 周报` 场景 —— 全库人群最宽的一条：

  1. 用户气泡打字机逐字出现：`把我这周记的工作内容整理成周报，正式一点，500 字以内`
  2. 顶部出现模式条：`智能模式 · 已为你选择` + 被选中的模型名，金色细标
  3. AI 回复流式逐段出现，结构化（本周完成 / 关键进展 / 遇到的问题 / 下周计划）
  4. 右上角浮出文件卡片：`周报-第34周.md 已存入文件库`，金色描边
  5. 停 2.5s → 淡出重播

- 循环播放；`prefers-reduced-motion` 下直接显示终态。
- 打字速度：用户气泡 45ms/字，AI 回复 12ms/字（明显快于用户输入，暗示"它很快"）。
- **窗口内容为重建，非截图**。理由：真截图是蓝白配色，铺进黑金页面必然打架；且静态图演不出流式过程与文件归档这两个动作。

---

### 4.2 交叉验证 · 凭什么是你

全站最重要的区块。差异化在这里落地。

**标题**：`三个模型背靠背作答，不一致的地方会被标出来`

**导语**：

> 单个 AI 在编的时候，不会告诉你它在编。
> 这不是能力问题——它只有一个视角，没法发现自己错了。

**呈现**：左文右图。

- 左：三条要点
  1. **多模型背靠背** —— 同一个问题独立作答，互不参考
  2. **自动合并去重** —— 一致的沉淀为共识
  3. **分歧单独标出** —— 不合成一个看起来很确定的结论
- 右：**真实截图 #1**（交叉验证结果界面，能看到共识与分歧标记），放入 `--surface` 内嵌框 + 金色细描边。

**落点文案**：

> 出错的地方会被标出来。

**⚠️ 话术约束**：不用「多模型对比」「暴力模式」「更强/最优」；不承诺「保证正确」。见 01 文档 §5.2。

---

### 4.3 定时任务 · 它自己来找你

**标题**：`你不用打开任何 App，它自己来微信找你`

**导语**：

> 别的 AI 是你不去问，它就不存在。
> Lucimix 的任务在云端按时执行，结果直接推到微信——手机锁屏也照样到。

**呈现**：左侧任务配置，右侧手机推送，中间一条金色连线表示"执行 → 送达"。

- 左：**真实截图 #4**（定时任务创建弹窗/列表），展示可配置项 —— 频率与时刻、推送渠道、每个任务单独指定模型
- 右：**真实截图 #3**（微信里收到推送的手机截图）。手机截图比网页截图可信度高得多，这是本区块的关键素材。
- 下方三个场景卡片（取自素材库组二）：
  - 每周五 16:00 · 周报写好发到微信
  - 每天 08:00 · 行业情报，各家说法不一致的会单独标出
  - 每天 20:00 · 面试每日一题

**⚠️ 前置依赖**：第二张场景卡（行业情报）依赖「定时任务执行时能否联网检索」，见 §8 待确认第 1 条。**未确认前不得上线该卡片。**

---

### 4.4 文件库 · 产出不会丢

**标题**：`AI 生成的东西，自动存进你的文件库`

**导语**：

> 对话关掉，产出还在。全部 AI 产出与你上传的文件汇总一处，可搜索、可预览、可下载，也能直接引用到新对话里——不用下载再上传。

**呈现**：**真实截图 #5**（文件库「全部」tab）居中大图，下方四个分类标注：全部 / 我的云盘 / 对话内文件 / 图片生成。

---

### 4.5 模型矩阵 · 用的是谁的模型

**标题**：`不绑定任何一家`

**呈现**：模型名网格，两行分组，`JetBrains Mono` 排版：

- **国产**：DeepSeek · Kimi · GLM · Qwen …
- **世界前沿**：Claude · GPT …

沿用登录页已上线的表述（01 文档 §2.1）。

**三种模式说明**，横向三卡：

| 模式 | 一句话 | 什么时候用 |
|---|---|---|
| 智能模式 | 系统自动选最合适的模型 | 长、日常、随手问、自己看 |
| 交叉验证 | 多个模型背靠背，分歧标出 | 短、重要、要对外交付、错了要担责 |
| 自定义模型 | 手动指定 | 明确知道某个模型在这件事上更强 |

判断标准直接写出来：**这段内容错了要不要担责？**（素材库原文，是全库最好用的一句话）

**⚠️** 表中「交叉验证」为对外名；产品 UI 内叫「暴力模式」，官网一律不出现。

---

### 4.6 生态应用 · 一个账号，一份光点

**标题**：`平台上的应用，不用各自注册、各自付费`

**导语**（两条，只讲这两条就够）：

> **一个账号通用。** 平台上的应用不用挨个注册登录。
> **一份光点通用。** 不管用哪个应用，消耗的都是同一份光点——不需要在每个应用里单独充值。

这是与"下载一堆 AI 小工具、每个都要单独付费"最直观的区别。

**三张应用卡**（取自 `应用点-生态应用.md`）：

| 应用 | 一句话 | 素材 |
|---|---|---|
| 口播视频 | 一张照片 + 一条参考视频 → 口播视频和配套文案 | 卡片图示（纯排版 + 图标） |
| 收盘复盘 | 收盘后自动整理复盘推到微信，各家说法不一致的会标出来 | 卡片图示（纯排版 + 图标） |
| 批量换装 | 一张自己的照片出一整季搭配图，人还是同一个人 | **真实截图 #8**（图片生成结果）；缺失则同样降级为卡片图示 |

三张卡视觉规格一致，仅第三张承载真实截图 —— 避免三张卡各用一种呈现方式导致区块散乱。

**⚠️ 三条硬性话术边界**（`应用点-生态应用.md`）：

1. **肖像权**：只能用本人照片或有授权的形象。物料中必须写明。绝不出现用明星/网红/他人照片生成的示例——风险落在平台。
2. **不承诺"和真人无差别"**。可以说「不用露脸、不用出镜」，不能说「看不出是 AI」。
3. **股票复盘定位为信息整理工具，不提供任何投资建议**。这句免责必须出现在卡片上，不能只放页脚。

---

### 4.7 尾部 CTA + Footer

**CTA 区**

- 标题：`现在开始用`
- 光点说明（承接导航「定价」锚点）：光点是平台统一的计费单位，微信/支付宝充值；**邀请好友，双方各得 1000 光点**
- 主 CTA：`免费开始使用`

**Footer**

- 北京光忽智能科技有限公司
- 京 ICP 备案号（**需向用户取实际备案号** —— 品牌开场页上是占位的 `京ICP备2025000000号`）
- 服务协议 · 隐私政策
- 企业版入口 → `biz.lucimix.com`（唯一一处指向 B 端，仅文字链，不做引流位）

---

## 5. 素材

### 5.1 分工

| 位置 | 用什么 | 为什么 |
|---|---|---|
| 首屏对话窗口 | **重建**的黑金版模拟对话 | 需要动、需要配色统一、需要演出流式与归档过程 |
| 首屏以下功能区 | **真实截图** | 这里要的是可信度，不是冲击力 |

### 5.2 待用户提供的截图

放入 `assets/screenshots/`。要求：产品有暗色主题的用暗色；宽 ≥ 1600px；真实内容非占位符；手机号/邮箱打码。

| # | 截图 | 用在 | 优先级 |
|---|---|---|---|
| 1 | 交叉验证结果界面（含共识与分歧标记） | §4.2 | **必需** —— 缺了整个差异化叙事落空 |
| 3 | 微信收到定时任务推送（手机截图） | §4.3 | **必需** |
| 4 | 定时任务创建弹窗 + 任务列表 | §4.3 | 必需 |
| 5 | 文件库「全部」tab | §4.4 | 必需 |
| 8 | 图片生成结果 | §4.6 | 需要 |
| 2 | 对话主界面 | 备用 | 可选 |
| 6 | 侧边栏（光点余额 + 邀请入口） | §4.7 | 可选 |
| 7 | API Key / 用量统计 | 本期不用 | 可选 |

**降级方案**：任一必需截图缺失时，该区块改用与首屏同法的重建版界面示意，并在 §8 记录待替换。不允许留空白框。

### 5.3 已有资产

- `assets/brand/lucimix-logo.svg` —— logo 标记，`#0052ff`
- `assets/brand/brand-splash-lucimix.com.html` —— 粒子 canvas 源码，直接移植
- 横版组合标（logo + wordmark）**尚无**。登录页上的 "lucimix" 小写字标需确认字体；未确认前用 Inter 小写、字距 `-0.02em` 临时替代。

---

## 6. 技术方案

**栈**：Vite + React + TypeScript，纯 CSS（CSS 自定义属性），无 UI 框架。

理由：

- 与产品端一致（两端都是 Vite + React），团队维护无切换成本，design token 可直接搬
- 页面高度美术导向，Tailwind 的类堆叠在这种页面上是负担
- P1 的应用点落地页要由 30 条素材数据驱动生成，React 在那一步明显更合适；首页就按同一套骨架起

**目录**

```
src/
  main.tsx
  App.tsx
  styles/
    tokens.css        # §3.1 色板 + §3.3 排版阶梯
    base.css          # reset、字体声明
  components/
    Nav.tsx
    Hero.tsx
    ChatSimulation.tsx    # 首屏模拟对话
    ParticleField.tsx     # 粒子 canvas
    CrossCheck.tsx        # §4.2
    ScheduledTasks.tsx    # §4.3
    FileLibrary.tsx       # §4.4
    ModelMatrix.tsx       # §4.5
    Ecosystem.tsx         # §4.6
    FinalCta.tsx          # §4.7
    Footer.tsx
    ui/                   # Button、SectionHeading、ScreenshotFrame、Reveal
  content/
    copy.ts               # 全站文案集中一处，便于审校与后续 i18n
  hooks/
    useTypewriter.ts
    useInViewOnce.ts
public/
  fonts/                  # 子集化后的思源宋体 woff2
assets/screenshots/
```

**文案集中在 `content/copy.ts`**：全站对外表述受 01 文档 §5 的禁区与话术纪律约束，集中管理才能一次性审校，也为将来英文版留口。

**构建期字体子集化**：`scripts/subset-fonts.mjs`，扫描 `copy.ts` 中出现在标题层级的汉字，用 `fonttools` 产出 woff2。目标 < 60KB。

**部署**：静态产物。

---

## 7. 响应式与性能

| 断点 | 处理 |
|---|---|
| ≥1280px | 完整布局，左右分栏区块横排 |
| 768–1279px | 左右分栏改上下堆叠，截图满宽 |
| <768px | 单列；导航折叠为汉堡（主 CTA 常驻）；首屏模拟对话窗口宽 `94vw` 并压缩内容行数；粒子数量降到上限 30 |

**性能**

- 首屏 LCP < 2.5s（4G）。LCP 元素是主标题文字，因此中文衬线必须子集化并 `font-display: swap`
- 截图导出 WebP，`<img loading="lazy" decoding="async">` + 显式宽高防抖动
- 粒子 canvas 离开视口即停 rAF；移动端降数量
- 模拟对话用 `IntersectionObserver` 触发，不在页面加载时就跑

**可访问性**

- 正文对比度 ≥ 4.5:1。在 `#000` 上实测：`--text-2` `#a3a3a8` ≈ 8.4:1，`--text-3` `#83838a` ≈ 5.6:1，两者均可用于正文
  - 注：初稿的 `--text-3` 取 `#6b6b70`，实测仅 ≈ 4.0:1，不达标，已上调至 `#83838a`
- 金色 `#d4b87a` 在 `#000` 上 ≈ 10.9:1，安全
- 主 CTA：`--blue` `#0052ff` 在 `#000` 上 ≈ 3.7:1 —— 达到非文本 UI 元素的 3:1 要求，可作按钮底与焦点环；按钮上的白色文字对蓝底 ≈ 5.8:1，达标。**蓝色不得用于黑底上的纯文字**
- 所有截图有中文 `alt`
- 键盘可达：跳转链接、焦点环用 `--blue`、锚点滚动后焦点落到目标区块标题
- 模拟对话对读屏器为 `aria-hidden`，另提供一段等价文字描述

---

## 8. 待确认

| # | 事项 | 影响 | 卡谁 |
|---|---|---|---|
| 1 | **定时任务执行时能否联网检索** | 决定 §4.3 第二张场景卡（每日行业情报）能否上线。素材库标为「必须先问清，不能带着不确定去投放」 | 研发 |
| 2 | **定时任务能否引用文件库文件** | 决定 §4.1 模拟对话演的周报场景（"我这周记的工作内容"）与 §4.3 卡片一的措辞 | 研发 |
| 3 | 主 CTA 落地 URL 与注册流程 | 假定 `https://lucimix.com/login`（手机号 + 短信验证码，未注册自动创建）。是否有游客试用？ | 用户 |
| 4 | ICP 备案号 | Footer。品牌页上是占位号 | 用户 |
| 5 | 「lucimix」小写字标的字体 | 组合标。未确认前用 Inter 临时替代 | 用户 |
| 6 | 首页是否需要真实定价数字（光点单价/套餐） | 当前 §4.7 只做机制说明，不列价格 | 用户 |
| 7 | 中文衬线是否接受思源宋体 | 若要延续 QingNiao 气质需另购授权 | 用户 |
| 8 | §5.2 必需截图 | 缺失则该区块降级为重建示意 | 用户 |

---

## 9. 合规红线（实现期不得逾越）

摘自 01 文档 §5，此处再列一遍，因为这是最容易在写文案时手滑的地方。

**严禁出现的能力表述**：Excel 处理 / **PPT 自动生成** / PDF 批量处理与格式转换 / 会议录音转写 / 本地文件操作与远程控制 / 建站部署 / Skill 市场与插件生态 / 漫剧绘本多角色分镜 / 降 AIGC 检测率与代写论文 / 「搭建工作台」。

**禁用词**：多模型对比、暴力模式、更强、最优、保证正确、看不出是 AI。

**必须出现的免责**：股票复盘卡片上的「不提供投资建议」；生态应用区的肖像权说明。

---

## 10. 验收标准

1. 七个区块全部实现，桌面与移动端均可用
2. 首屏在 1440×900 内不滚动即可看到标题、副标题、双 CTA、对话窗口顶部
3. 模拟对话在首屏进入视口后自动播放并循环；`prefers-reduced-motion` 下显示终态
4. 全站文案集中在 `content/copy.ts`，且通过 §9 红线人工审校
5. 主 CTA 在导航、首屏、尾部三处出现，指向同一 URL
6. 无死链（「定价」为锚点）
7. Lighthouse 移动端 Performance ≥ 85，Accessibility ≥ 95
8. 中文衬线子集 woff2 < 60KB

---

> 文档版本：v1.0 | 2026-08-19
